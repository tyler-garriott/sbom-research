{
  "name": "SBOM Research",
  "build": {
    "dockerfile": "../docker/Dockerfile",
    "context": "../docker"
  },
  "workspaceFolder": "/work",
  "workspaceMount": "source=${localWorkspaceFolder},target=/work,type=bind",
  "postCreateCommand": "syft version && gcc --version && jq --version",
  "customizations": {
    "zed": {
      "extensions": []
    },
    "vscode": {
      "extensions": []
    }
  }
}
